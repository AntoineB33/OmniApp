<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- PRD §15: the pause-end voice cue fires as an exact AlarmManager alarm (PauseCueAlarmReceiver). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- PRD §15 (1.6.0): one-time first-startup prompt to exempt the app from Doze, so the foreground
         service can keep the presence WebSocket alive while the phone is unlocked (MainActivity). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- allowBackup MUST stay false: OS auto-backup restores the app's data across uninstall+reinstall
         (old device id, stale rotated-out Supabase tokens, and pre-wipe device_active_session rows). That
         resurrected "own activity" from before an account-empty - rendering a phantom activity gap in the
         Inactivity bands and re-seeding the just-emptied server. Durable state syncs via Supabase; local
         state is per-install and must die with the install. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|density|uiMode|navigation|keyboard|fontScale|layoutDirection|locale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".SchedulerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the personal task scheduler running so reminders and look-away cues fire while the UI is closed." />
        </service>

        <!-- Quick Settings tile: a launcher shortcut in the notification shade that opens the app. -->
        <service
            android:name=".OpenAppTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_tile"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- PRD §15 / ARCHITECTURE.md §8: receives the pause-cue data push (schedule/cancel the local cue). -->
        <service
            android:name=".PauseCueMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- PRD §15: fires at the scheduled pause-end instant to run the eligibility gate + speak the cue. -->
        <receiver
            android:name=".PauseCueAlarmReceiver"
            android:enabled="true"
            android:exported="false" />
    </application>

</manifest>