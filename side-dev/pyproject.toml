[project]
name = "side-dev"
version = "0.1.0"
description = "Side developments: reference implementations used to try out scheduling logic before porting it to the app."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.11.1",
    "pulp>=3.3.2",
]
